import PendingBackend from '../components/PendingBackend'

export default function MonitoresPage() {
  return (
    <PendingBackend
      title="Monitores"
      description="Personal de monitoría — listado y gestión"
      backendPath="/monitores/"
    />
  )
}
